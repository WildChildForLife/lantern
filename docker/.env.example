# Copy to .env to override. Both have working defaults.

# Must support tool calling, or the agents produce no tool_use entries and the
# most interesting half of each adapter goes untested.
OLLAMA_MODEL=qwen3:0.6b

# Host port for the Lantern UI.
LANTERN_PORT=3410
