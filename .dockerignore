# VCS metadata
.git
.gitignore

# Local dependencies and caches
node_modules
.pnpm-store

# Build artifacts
dist
build
.turbo
.cache

# Test fixtures and docs not needed in image.
# All of fixtures/, not just the Claude tree — the others were being copied
# into every build context for nothing.
fixtures
docs
coverage

# The CLI test harness. It builds this image, so copying itself in would put a
# stale copy of the harness inside every release image.
docker

# Editor and OS files
.DS_Store
.idea
.vscode

# Logs and environment files
*.log
npm-debug.log*
pnpm-debug.log*
.env
.env.*
